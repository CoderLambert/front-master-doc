# 实战项目：表单应用 (Form)

import { LiveCode } from '@components';

## 项目概述

表单是 Web 应用的核心部分。本项目将帮助您掌握：

- 受控组件
- 表单验证
- 状态管理
- 条件渲染
- 错误处理
- 用户体验优化

## 基础表单

<LiveCode
  code={`
function ContactForm() {
  const [formData, setFormData] = useState({
    name: '',
    email: '',
    message: ''
  });
  const [errors, setErrors] = useState({});
  const [isSubmitting, setIsSubmitting] = useState(false);

  const handleChange = (e) => {
    const { name, value } = e.target;
    setFormData(prev => ({
      ...prev,
      [name]: value
    }));
    // 清除对应字段的错误
    if (errors[name]) {
      setErrors(prev => ({
        ...prev,
        [name]: ''
      }));
    }
  };

  const validate = (data) => {
    const newErrors = {};
    if (!data.name.trim()) newErrors.name = '请输入姓名';
    if (!data.email.trim()) {
      newErrors.email = '请输入邮箱';
    } else if (!/^\\S+@\\S+\\.\\S+$/.test(data.email)) {
      newErrors.email = '邮箱格式不正确';
    }
    if (!data.message.trim()) {
      newErrors.message = '请输入留言';
    } else if (data.message.length < 10) {
      newErrors.message = '留言至少10个字符';
    }
    return newErrors;
  };

  const handleSubmit = (e) => {
    e.preventDefault();
    const newErrors = validate(formData);

    if (Object.keys(newErrors).length > 0) {
      setErrors(newErrors);
      return;
    }

    setIsSubmitting(true);

    // 模拟提交
    setTimeout(() => {
      alert('表单提交成功！');
      setFormData({ name: '', email: '', message: '' });
      setErrors({});
      setIsSubmitting(false);
    }, 2000);
  };

  return (
    <div style={{
      maxWidth: '600px',
      margin: '40px auto',
      padding: '30px',
      backgroundColor: 'white',
      borderRadius: '12px',
      boxShadow: '0 4px 6px rgba(0,0,0,0.1)',
      fontFamily: 'Arial, sans-serif'
    }}>
      <h2 style={{ textAlign: 'center', color: '#333', marginBottom: '30px' }}>
        📝 联系我们
      </h2>

      <form onSubmit={handleSubmit}>
        {/* 姓名字段 */}
        <div style={{ marginBottom: '20px' }}>
          <label style={{ display: 'block', marginBottom: '8px', color: '#333', fontWeight: 'bold' }}>
            姓名 *
          </label>
          <input
            type="text"
            name="name"
            value={formData.name}
            onChange={handleChange}
            style={{
              width: '100%',
              padding: '12px',
              fontSize: '16px',
              border: errors.name ? '2px solid #f44336' : '2px solid #ddd',
              borderRadius: '6px',
              outline: 'none',
              boxSizing: 'border-box'
            }}
            placeholder="请输入您的姓名"
          />
          {errors.name && (
            <p style={{ color: '#f44336', fontSize: '14px', margin: '5px 0 0 0' }}>
              {errors.name}
            </p>
          )}
        </div>

        {/* 邮箱字段 */}
        <div style={{ marginBottom: '20px' }}>
          <label style={{ display: 'block', marginBottom: '8px', color: '#333', fontWeight: 'bold' }}>
            邮箱 *
          </label>
          <input
            type="email"
            name="email"
            value={formData.email}
            onChange={handleChange}
            style={{
              width: '100%',
              padding: '12px',
              fontSize: '16px',
              border: errors.email ? '2px solid #f44336' : '2px solid #ddd',
              borderRadius: '6px',
              outline: 'none',
              boxSizing: 'border-box'
            }}
            placeholder="example@email.com"
          />
          {errors.email && (
            <p style={{ color: '#f44336', fontSize: '14px', margin: '5px 0 0 0' }}>
              {errors.email}
            </p>
          )}
        </div>

        {/* 留言字段 */}
        <div style={{ marginBottom: '30px' }}>
          <label style={{ display: 'block', marginBottom: '8px', color: '#333', fontWeight: 'bold' }}>
            留言 *
          </label>
          <textarea
            name="message"
            value={formData.message}
            onChange={handleChange}
            rows="6"
            style={{
              width: '100%',
              padding: '12px',
              fontSize: '16px',
              border: errors.message ? '2px solid #f44336' : '2px solid #ddd',
              borderRadius: '6px',
              outline: 'none',
              resize: 'vertical',
              boxSizing: 'border-box',
              fontFamily: 'inherit'
            }}
            placeholder="请输入您的留言..."
          />
          {errors.message && (
            <p style={{ color: '#f44336', fontSize: '14px', margin: '5px 0 0 0' }}>
              {errors.message}
            </p>
          )}
          <p style={{ color: '#999', fontSize: '12px', margin: '5px 0 0 0' }}>
            {formData.message.length}/500
          </p>
        </div>

        {/* 提交按钮 */}
        <button
          type="submit"
          disabled={isSubmitting}
          style={{
            width: '100%',
            padding: '15px',
            fontSize: '18px',
            fontWeight: 'bold',
            backgroundColor: isSubmitting ? '#9e9e9e' : '#2196f3',
            color: 'white',
            border: 'none',
            borderRadius: '6px',
            cursor: isSubmitting ? 'not-allowed' : 'pointer',
            transition: 'background-color 0.3s'
          }}
        >
          {isSubmitting ? '提交中...' : '提交表单'}
        </button>
      </form>
    </div>
  );
}
`}
/>

## 学习要点

1. **受控组件** - 表单值由 React state 控制
2. **表单验证** - 实时验证用户输入
3. **错误处理** - 显示友好的错误信息
4. **状态管理** - 管理表单数据、错误和提交状态
5. **用户体验** - 禁用按钮、加载状态等

## 扩展挑战

1. 添加更多字段（电话、地址等）
2. 实现文件上传
3. 添加验证码
4. 使用 useReducer 重构
5. 添加表单保存草稿功能
